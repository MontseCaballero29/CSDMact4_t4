info:
  name: 08 - Eliminar producto
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/productos/{{productoId}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
