info:
  name: 06 - Obtener producto por ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/productos/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
