info:
  name: 10 - Validación incorrecta
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/productos"
  body:
    type: json
    data: |-
      {
        "nombre": "",
        "descripcion": "",
        "precio": 0,
        "stock": -5,
        "categoriaId": 0
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
