info:
  name: 03 - Crear categoría
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/categorias"
  body:
    type: json
    data: |-
      {
        "nombre": "Pasteles"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
